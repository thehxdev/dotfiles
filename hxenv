#!/usr/bin/env bash

export OPT="$HOME/opt"
export OPT_GLOBAL="$OPT/_global"

__GLOBAL_PATH=""
for p in $(ls -1 $OPT_GLOBAL); do
    __absolute_path="$OPT_GLOBAL/$p/bin"
    if [[ ! "$PATH" == *"$__absolute_path"* ]]; then
        __GLOBAL_PATH+="$__absolute_path:"
    fi
done

export PATH="$PATH:$HOME/.local/bin:$__GLOBAL_PATH"
export MANPAGER="nvim +Man!"

alias nv='nvim'
alias tm='tmux'
alias ll='eza -lh --group-directories-first'
alias la='eza -lha --group-directories-first'
alias cdp='cd ~/projects'
alias cdo='cd ~/opt'
